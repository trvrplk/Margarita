==Make a site with XML
       _Margarita_ generates CSS for XML to become a site. 

==Files required _Before_ Margsrita-fying  
	You will need a *index.htm* file, and *an RSS feed*. I recommend my oth product RSSify[http://cookiebatch.github.com/RSSify/].